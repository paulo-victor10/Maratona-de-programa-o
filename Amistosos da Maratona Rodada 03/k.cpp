#include <bits/stdc++.h>
using ll=long long;
using namespace std;

int main(){
    int t;
    cin>>t;
    while (t--)
    {
        int n, p,q;
        cin>>n>>p>>q;
        vector<ll> pos(n);
        for (int i = 0; i < n; i++)
        {
            cin>>pos[i];
        }
    }
}