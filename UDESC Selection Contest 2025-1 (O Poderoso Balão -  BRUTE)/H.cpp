#include <bits/stdc++.h>

using namespace std;

using ll=long long;

const int TAM = 500005;

vector<int> tree[TAM];

ll a[TAM];
ll dp[TAM];
ll sum_sub[TAM];

void dfs (int u){
    sum_sub[u] = a[u];
    ll
}